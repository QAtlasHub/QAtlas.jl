# 🔵 `XXZ1D/FreeEnergy/Infinite`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! note "Assurance level: coherent"
    An independent card exists and the value satisfies an internal invariant; no external value re-derives it yet.

## `src` claim

- method `klumper_nlie`, status `exact`, reliability `medium`, refs: Mahan2000 | Coleman2015 | Takahashi1999 | Klumper1993
- Δ = 0: XX free-fermion f(β) by QuadGK (exact); -1 < Δ < 1, |Δ| < 0.99: Klümper QTM NLIE (issue #521); |Δ| ≥ 0.99 or gapped: NaN + warn.

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@free_fermion` | `sum_rule` | 🟡 asserted | Gibbs identity: f = e - s/beta (thermodynamic sum rule) | `test/models/quantum/XXZ/test_xxz_xx_infinite.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(XXZ1D(; J = 1.0, Δ = 0.0), FreeEnergy(), Infinite(); route = :sum_rule, fetch_kw = (; 1.0 = 1.0), independent = QAtlas.fetch(XXZ1D(; J = 1.0, Δ = 0.0), Energy(), Infinite(); 1.0 = 1.0) - QAtlas.fetch(XXZ1D(; J = 1.0, Δ = 0.0), ThermalEntropy(), Infinite(); 1.0 = 1.0) / 1.0, agree_within = 1.0e-9, refs = ["Gibbs identity: f = e - s/beta (thermodynamic sum rule)"])
```


## Assurance (provisional)

- level: **coherent** 🔵
- cards: 1 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `XXZ1D`](../models/XXZ1D.md) · [Quantity: `FreeEnergy`](../quantities/FreeEnergy.md) · [Atlas index](../index.md)
