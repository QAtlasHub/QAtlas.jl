# 🟢 `TFIM/SpecificHeat/OBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `bdg`, reliability `high`

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC, T → ∞: c → 0 as β² (high-T tail) | `test/models/quantum/TFIM/test_TFIM_specific_heat_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `sum_rule` | 🟡 asserted | c_v = -β² ∂ε/∂β on OBC(N) — Energy(OBC) is total ⇒ divide by N; per-site SpecificHeat by convention | `test/verification/universality/test_thermo_identity_quantum.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 0.5 = 0.5, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 0.5 = 0.5, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 0.5 = 0.5, 2.0 = 2.0), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J), T → 0: c = 0 exactly (field-induced gap exponentially suppresses c)"], fetch_kw = (; beta = 100.0))
```

```julia
verify(TFIM(; 0.5 = 0.5, 2.0 = 2.0), SpecificHeat(), OBC(12); route = :limiting_case, independent = 0.0, agree_within = 0.0001, refs = ["TFIM OBC, T → ∞: c → 0 as β² (high-T tail)"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(4); route = :ed_finite_size, independent = ed_C, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(6); route = :ed_finite_size, independent = ed_C, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SpecificHeat(), OBC(8); route = :ed_finite_size, independent = ed_C, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (C)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; J = J, h = h), SpecificHeat(), OBC((4, 6, 8)); route = :sum_rule, fetch_kw = (; beta = (0.5, 1.0, 2.0)), independent = (-((0.5, 1.0, 2.0) ^ 2) * ForwardDiff.derivative((b->begin QAtlas.fetch(TFIM(; J = J, h = h), Energy(), OBC((4, 6, 8)); beta = b) end), (0.5, 1.0, 2.0))) / (4, 6, 8), agree_within = 1.0e-9, at = ["J=$(J)", "h=$(h)", "N=$((4, 6, 8))", "β=$((0.5, 1.0, 2.0))"], refs = ["c_v = -β² ∂ε/∂β on OBC(N) — Energy(OBC) is total ⇒ divide by N; per-site SpecificHeat by convention"])
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 40 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `TFIM`](../models/TFIM.md) · [Quantity: `SpecificHeat`](../quantities/SpecificHeat.md) · [Atlas index](../index.md)
