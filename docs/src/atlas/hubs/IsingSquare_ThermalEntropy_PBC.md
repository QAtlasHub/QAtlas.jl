# 🟢 `IsingSquare/ThermalEntropy/PBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `central_diff`, reliability `medium`
- s = β(ε - f).

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@onsager` | `ed_finite_size` | 🟢 structural | Brute-force ED: S/N = β·(E-F)/N over all configs | `test/models/classical/test_IsingSquare_PBC_thermo_ED_batch.jl` |
| `@onsager` | `ed_finite_size` | 🟢 structural | Brute-force ED: S/N = β·(E-F)/N over all configs | `test/models/classical/test_IsingSquare_PBC_thermo_ED_batch.jl` |
| `@onsager` | `ed_finite_size` | 🟢 structural | Brute-force ED: S/N = β·(E-F)/N over all configs | `test/models/classical/test_IsingSquare_PBC_thermo_ED_batch.jl` |
| `@onsager` | `ed_finite_size` | 🟢 structural | Brute-force ED: S/N = β·(E-F)/N over all configs | `test/models/classical/test_IsingSquare_PBC_thermo_ED_batch.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(IsingSquare(; 0.5 = 0.5, 3 = 3, 3 = 3), ThermalEntropy(), PBC(); route = :ed_finite_size, independent = ed_S, at = ["LxLy=$(3)x$(3)"], agree_within = 1.0e-9, refs = ["Brute-force ED: S/N = β·(E-F)/N over all configs"], fetch_kw = (; 0.3 = 0.3))
```

```julia
verify(IsingSquare(; 0.5 = 0.5, 3 = 3, 3 = 3), ThermalEntropy(), PBC(); route = :ed_finite_size, independent = ed_S, at = ["LxLy=$(3)x$(3)"], agree_within = 1.0e-9, refs = ["Brute-force ED: S/N = β·(E-F)/N over all configs"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(IsingSquare(; 1.0 = 1.0, 3 = 3, 3 = 3), ThermalEntropy(), PBC(); route = :ed_finite_size, independent = ed_S, at = ["LxLy=$(3)x$(3)"], agree_within = 1.0e-9, refs = ["Brute-force ED: S/N = β·(E-F)/N over all configs"], fetch_kw = (; 0.3 = 0.3))
```

```julia
verify(IsingSquare(; 1.0 = 1.0, 3 = 3, 3 = 3), ThermalEntropy(), PBC(); route = :ed_finite_size, independent = ed_S, at = ["LxLy=$(3)x$(3)"], agree_within = 1.0e-9, refs = ["Brute-force ED: S/N = β·(E-F)/N over all configs"], fetch_kw = (; 1.0 = 1.0))
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 4 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `IsingSquare`](../models/IsingSquare.md) · [Quantity: `ThermalEntropy`](../quantities/ThermalEntropy.md) · [Atlas index](../index.md)
