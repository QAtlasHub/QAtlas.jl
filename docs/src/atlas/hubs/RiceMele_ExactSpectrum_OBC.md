# 🔵 `RiceMele/ExactSpectrum/OBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! note "Assurance level: coherent"
    An independent card exists and the value satisfies an internal invariant; no external value re-derives it yet.

## `src` claim

- method `single_particle_diagonalization`, status `exact`, reliability `high`, refs: RiceMele1982
- "ALL 2N single-particle energies, not SSH's non-negative half: the OBC ± symmetry " * "holds only while the unit cells are complete."

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²) | `test/models/quantum/misc/test_ricemele.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(RiceMele(; 1.0, w = 0.0, 0.0), ExactSpectrum(), OBC(4); route = :limiting_case, independent = (2 * 4) * sqrt(1.0 ^ 2 + 0.0 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 1.0, w = 0.0, 0.0), ExactSpectrum(), OBC(9); route = :limiting_case, independent = (2 * 9) * sqrt(1.0 ^ 2 + 0.0 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 1.0, w = 0.0, 0.3), ExactSpectrum(), OBC(4); route = :limiting_case, independent = (2 * 4) * sqrt(1.0 ^ 2 + 0.3 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 1.0, w = 0.0, 0.3), ExactSpectrum(), OBC(9); route = :limiting_case, independent = (2 * 9) * sqrt(1.0 ^ 2 + 0.3 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 0.4, w = 0.0, 0.9), ExactSpectrum(), OBC(4); route = :limiting_case, independent = (2 * 4) * sqrt(0.4 ^ 2 + 0.9 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 0.4, w = 0.0, 0.9), ExactSpectrum(), OBC(9); route = :limiting_case, independent = (2 * 9) * sqrt(0.4 ^ 2 + 0.9 ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 2.0, w = 0.0, -0.5), ExactSpectrum(), OBC(4); route = :limiting_case, independent = (2 * 4) * sqrt(2.0 ^ 2 + (-0.5) ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```

```julia
verify(RiceMele(; 2.0, w = 0.0, -0.5), ExactSpectrum(), OBC(9); route = :limiting_case, independent = (2 * 9) * sqrt(2.0 ^ 2 + (-0.5) ^ 2), agree_within = 1.0e-10, subject_extract = (s->begin sum(abs, s) end), refs = ["w = 0: the 2N levels are ±√(v²+Δ²), so Σ|ε| = 2N√(v²+Δ²)"])
```


## Assurance (provisional)

- level: **coherent** 🔵
- cards: 8 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `RiceMele`](../models/RiceMele.md) · [Quantity: `ExactSpectrum`](../quantities/ExactSpectrum.md) · [Atlas index](../index.md)
