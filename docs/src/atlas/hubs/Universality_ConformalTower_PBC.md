# 🟢 `Universality/ConformalTower/PBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `analytic`, status `exact`, reliability `high`, refs: Cardy1986
- Conformal tower of states excitation energies E_n - E_0 = (2π v / L) Δ_n under PBC.

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `second_closed_form` | 🟢 structural | Cardy 1986: Ising PBC spin state dimension Δ = 1/8 | `test/universalities/test_universality_conformal_tower.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | Cardy 1986 / Affleck 1986: Heisenberg PBC spinon state dimension Δ = 1/4 | `test/universalities/test_universality_conformal_tower.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(Universality(:Ising), ConformalTower(), PBC(); route = :second_closed_form, independent = (0.25 * π * 2.0) / 16.0, agree_within = 1.0e-12, refs = ["Cardy 1986: Ising PBC spin state dimension Δ = 1/8"], fetch_kw = (; L = 16.0, v = 2.0), subject_extract = (raw->begin (raw[2]).energy end))
```

```julia
verify(Universality(:Heisenberg), ConformalTower(), PBC(); route = :second_closed_form, independent = (0.5 * π * 2.0) / 16.0, agree_within = 1.0e-12, refs = ["Cardy 1986 / Affleck 1986: Heisenberg PBC spinon state dimension Δ = 1/4"], fetch_kw = (; L = 16.0, v = 2.0), subject_extract = (raw->begin (raw[2]).energy end))
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 2 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `Universality`](../models/Universality.md) · [Quantity: `ConformalTower`](../quantities/ConformalTower.md) · [Atlas index](../index.md)
