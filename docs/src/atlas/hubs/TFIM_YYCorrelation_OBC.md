# 🟢 `TFIM/YYCorrelation/OBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the registry claims + static `test/INVENTORY.jsonl`. No source/test executed; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired); `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `pfaffian`, reliability `high`
- σʸ_i = -(-i)^{i-1} γ_1 … γ_{2i-2} γ_{2i}; same Pfaffian machinery as σᶻ.

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `ed_finite_size` | 🟢 structural | Direct OBC dense-ED ⟨σy_i σy_j⟩ via _build_tfim_dense ground state | `test/models/quantum/TFIM/test_TFIM_yy.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(TFIM(; J = J, h = h), YYCorrelation(; mode = :static), OBC(N); route = :ed_finite_size, fetch_kw = (; i = i, j = j, beta = Inf), independent = yy_ed, agree_within = 1.0e-8, refs = ["Direct OBC dense-ED ⟨σy_i σy_j⟩ via _build_tfim_dense ground state"])
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 1 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.

[← back to the Atlas index](../index.md)
