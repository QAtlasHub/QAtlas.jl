# 🟢 `RiceMele/CorrelationLength/Infinite`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `analytic`, status `exact`, reliability `high`, refs: RiceMele1982
- ξ = 1/√((|v|−|w|)² + Δ²). Finite for every Δ ≠ 0, unlike SSH on |v| = |w|.

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |
| `@sweep` | `second_closed_form` | 🟢 structural | ξ = 1/gap, with the gap taken from the same grid minimum | `test/models/quantum/misc/test_ricemele.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(RiceMele(; 0.75, 0.25, 0.1), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt(0.75 ^ 2 + 0.25 ^ 2 + 2 * 0.75 * 0.25 * cos(k) + 0.1 ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```

```julia
verify(RiceMele(; 0.25, 0.75, 0.1), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt(0.25 ^ 2 + 0.75 ^ 2 + 2 * 0.25 * 0.75 * cos(k) + 0.1 ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```

```julia
verify(RiceMele(; 1.0, 1.0, 0.3), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt(1.0 ^ 2 + 1.0 ^ 2 + 2 * 1.0 * 1.0 * cos(k) + 0.3 ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```

```julia
verify(RiceMele(; -0.5, 0.7, 0.2), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt((-0.5) ^ 2 + 0.7 ^ 2 + 2 * -0.5 * 0.7 * cos(k) + 0.2 ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```

```julia
verify(RiceMele(; 0.6, -1.0, -0.4), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt(0.6 ^ 2 + (-1.0) ^ 2 + 2 * 0.6 * -1.0 * cos(k) + (-0.4) ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```

```julia
verify(RiceMele(; 1.0, 0.0, 0.5), CorrelationLength(), Infinite(); route = :second_closed_form, independent = 1 / minimum((sqrt(1.0 ^ 2 + 0.0 ^ 2 + 2 * 1.0 * 0.0 * cos(k) + 0.5 ^ 2) for k = range(-π, π; length = 400001))), agree_within = 1.0e-8, refs = ["ξ = 1/gap, with the gap taken from the same grid minimum"])
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 6 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `RiceMele`](../models/RiceMele.md) · [Quantity: `CorrelationLength`](../quantities/CorrelationLength.md) · [Atlas index](../index.md)
