# 🟢 `XXZ1D/GroundStateEnergyDensity/Infinite`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `bethe_ansatz`, status `exact`, reliability `high`, refs: YangYang1969 | desCloizeauxPearson1962
- e₀(Δ) at the isotropic AF point (Δ=1) reduces to Heisenberg1D Hulthén value J(1/4-ln 2); at the FM point (Δ=-1) the aligned state is exact, e₀ = -J/4.

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@fm` | `second_closed_form` | 🟢 structural | XXZ FM point Δ=-1: aligned state exact, e0 = -J/4 | `test/identities/test_identities_XXZ1D.jl` |
| `@su2` | `delegation_invariant` | 🟡 asserted | XXZ1D(Δ=1) ≡ Heisenberg1D: independent code paths must agree | `test/identities/test_identities_XXZ1D.jl` |
| `@su2` | `delegation_invariant` | 🟡 asserted | XXZ1D at Delta=1 === Heisenberg1D: two independent code paths must agree | `test/models/quantum/XXZ/test_XXZ1D.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(XXZ1D(; J = 1.0, Δ = -1.0), GroundStateEnergyDensity(), Infinite(); route = :second_closed_form, independent = -0.25, agree_within = 1.0e-12, refs = ["XXZ FM point Δ=-1: aligned state exact, e0 = -J/4"])
```

```julia
verify(XXZ1D(; J = 1.0, Δ = 1.0), GroundStateEnergyDensity(), Infinite(); route = :delegation_invariant, independent = QAtlas.fetch(Heisenberg1D(), GroundStateEnergyDensity(), Infinite()), agree_within = 1.0e-12, refs = ["XXZ1D(Δ=1) ≡ Heisenberg1D: independent code paths must agree"])
```

```julia
verify(XXZ1D(; J = 1.0, Δ = 1.0), GroundStateEnergyDensity(), Infinite(); route = :delegation_invariant, independent = QAtlas.fetch(Heisenberg1D(), GroundStateEnergyDensity(), Infinite()), agree_within = 1.0e-12, refs = ["XXZ1D at Delta=1 === Heisenberg1D: two independent code paths must agree"])
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 3 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `XXZ1D`](../models/XXZ1D.md) · [Quantity: `GroundStateEnergyDensity`](../quantities/GroundStateEnergyDensity.md) · [Atlas index](../index.md)
