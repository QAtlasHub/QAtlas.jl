# 🟢 `TFIM/SusceptibilityXX/OBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `bdg`, reliability `high`

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)" | `test/models/quantum/TFIM/test_TFIM_susc_obc_ED_batch.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 0.5, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 0.5, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 0.5, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 0.5, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 0.5, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 0.5, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 1.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 1.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 1.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 1.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 1.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 1.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 2.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(4); route = :ed_finite_size, independent = ed_tfim_chi(4, 1.0, 2.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 2.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(6); route = :ed_finite_size, independent = ed_tfim_chi(6, 1.0, 2.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 2.0, 1.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 1.0 = 1.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), SusceptibilityXX(), OBC(8); route = :ed_finite_size, independent = ed_tfim_chi(8, 1.0, 2.0, 5.0, 2 * (spin_ops(1 // 2))[1]), at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: build H_TFIM from scratch with spin_ops(1/2), diagonalise, compute beta*Var(M_alpha)/N (alpha=x)"], fetch_kw = (; 5.0 = 5.0))
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 18 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `TFIM`](../models/TFIM.md) · [Quantity: `SusceptibilityXX`](../quantities/SusceptibilityXX.md) · [Atlas index](../index.md)
