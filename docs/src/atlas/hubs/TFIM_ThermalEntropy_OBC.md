# 🟢 `TFIM/ThermalEntropy/OBC`

!!! note "Provisional v2 view — RES not wired"
    Generated by `docs/atlas/generate.jl` — a pure VIEW over the `*_registry.jl` claims + the static `test/INVENTORY.jsonl` AST scan. **No test is executed and no `src` is run**; `test/INVENTORY.jsonl` is regenerated in-place (idempotently) from that static scan; `fetch`/`@register` untouched. Assurance labels are PROVISIONAL: residuals / confidence are not shown yet (RES not wired). Badges reflect the **committed test AST**, not the latest CI run — a hub can read green while its `@test` is red between regenerations. `@sweep` = a graceful regime-resolution gap, not card omission.

!!! tip "Assurance level: corroborated-at-p"
    Independently corroborated. See the cards below.

## `src` claim

- method `bdg`, reliability `high`

## Corroboration

| regime | mechanism | independence | refs | file |
|---|---|---|---|---|
| `@ordered` | `sum_rule` | 🟡 asserted | Gibbs identity s = β(ε − f) across Energy/FreeEnergy/Entropy paths | `test/identities/test_property_invariants.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue) | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC at T → ∞: s → log 2 per site | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC at T → ∞: s → log 2 per site | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `limiting_case` | 🟡 asserted | TFIM OBC at T → ∞: s → log 2 per site | `test/models/quantum/TFIM/test_TFIM_thermal_entropy_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `ed_finite_size` | 🟢 structural | "ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)" | `test/models/quantum/TFIM/test_TFIM_thermo_OBC_ED_batch.jl` |
| `@sweep` | `sum_rule` | 🟡 asserted | Gibbs identity s = β(ε − f) on OBC(N) — Energy is total ⇒ ε = E/N; FreeEnergy / ThermalEntropy are per-site by convention | `test/verification/universality/test_thermo_identity_quantum.jl` |

## Test calls

_The exact `verify(...)` call the harness executed for this hub (reconstructed from the test AST):_

```julia
verify(TFIM(; J = 1.0, h = 0.5), ThermalEntropy(), OBC(6); route = :sum_rule, fetch_kw = (; beta = 1.3), independent = 1.3 * (QAtlas.fetch(TFIM(; J = 1.0, h = 0.5), Energy(:per_site), OBC(6); beta = 1.3) - QAtlas.fetch(TFIM(; J = 1.0, h = 0.5), FreeEnergy(), OBC(6); beta = 1.3)), agree_within = 1.0e-7, refs = ["Gibbs identity s = β(ε − f) across Energy/FreeEnergy/Entropy paths"])
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(; 8 = 8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(; 12 = 12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(; 16 = 16); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(; 8 = 8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(; 12 = 12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(; 16 = 16); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 0.5 = 0.5, 1.0 = 1.0), ThermalEntropy(), OBC(; 8 = 8); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 0.5 = 0.5, 1.0 = 1.0), ThermalEntropy(), OBC(; 12 = 12); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 0.5 = 0.5, 1.0 = 1.0), ThermalEntropy(), OBC(; 16 = 16); route = :limiting_case, independent = 0.0, agree_within = 1.0e-9, refs = ["TFIM OBC disordered phase (h>J) unique GS at T → 0 ⇒ s = 0 (gap = 2(h-J), no doublet residue)"], fetch_kw = (; beta = 1000.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(; 8 = 8); route = :limiting_case, independent = log(2), agree_within = 1.0e-5, refs = ["TFIM OBC at T → ∞: s → log 2 per site"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(; 12 = 12); route = :limiting_case, independent = log(2), agree_within = 1.0e-5, refs = ["TFIM OBC at T → ∞: s → log 2 per site"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(; 16 = 16); route = :limiting_case, independent = log(2), agree_within = 1.0e-5, refs = ["TFIM OBC at T → ∞: s → log 2 per site"], fetch_kw = (; beta = 0.001))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 0.5 = 0.5), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 1.0 = 1.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(4); route = :ed_finite_size, independent = ed_S, at = ["N=$(4)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(6); route = :ed_finite_size, independent = ed_S, at = ["N=$(6)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 0.5 = 0.5))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 2.0 = 2.0))
```

```julia
verify(TFIM(; 1.0 = 1.0, 2.0 = 2.0), ThermalEntropy(), OBC(8); route = :ed_finite_size, independent = ed_S, at = ["N=$(8)"], agree_within = 1.0e-9, refs = ["ED black-box: -J σ_z⊗σ_z bonds + -h σ_x onsite, full spectrum, thermo_from_spectrum (S)"], fetch_kw = (; 5.0 = 5.0))
```

```julia
verify(TFIM(; J = J, h = h), ThermalEntropy(), OBC((4, 6, 8)); route = :sum_rule, fetch_kw = (; beta = (0.5, 1.0, 2.0)), independent = (0.5, 1.0, 2.0) * (QAtlas.fetch(TFIM(; J = J, h = h), Energy(), OBC((4, 6, 8)); beta = (0.5, 1.0, 2.0)) / (4, 6, 8) - QAtlas.fetch(TFIM(; J = J, h = h), FreeEnergy(), OBC((4, 6, 8)); beta = (0.5, 1.0, 2.0))), agree_within = 1.0e-10, at = ["J=$(J)", "h=$(h)", "N=$((4, 6, 8))", "β=$((0.5, 1.0, 2.0))"], refs = ["Gibbs identity s = β(ε − f) on OBC(N) — Energy is total ⇒ ε = E/N; FreeEnergy / ThermalEntropy are per-site by convention"])
```


## Assurance (provisional)

- level: **corroborated-at-p** 🟢
- cards: 41 · model ED-feasible
- RES not wired — measured residuals / confidence are not shown yet.


[← Model: `TFIM`](../models/TFIM.md) · [Quantity: `ThermalEntropy`](../quantities/ThermalEntropy.md) · [Atlas index](../index.md)
